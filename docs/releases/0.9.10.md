# Host Monitoring Server 0.9.10

发布流水线现在把只读构建与最小权限发布分离：标签制品完成版本、来源和不可变发行树验证后，由独立 `contents: write` 任务自动创建 GitHub Release；已有 Release 会被拒绝覆盖。

服务端同时接受 Host Monitoring Client 0.9.10 至 0.9.14 的当前报告身份，保持既有 0.9.3 至 0.9.9 客户端兼容范围。
