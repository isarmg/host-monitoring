# host-monitoring-server 0.9.6

管理界面右上角新增圆形人物图标，可验证当前密码后修改当前账号名称及密码。保存后所有旧会话失效，使用新账号重新登录。账号 ID、业务数据和现有数据库格式保持不变。

统一使用 Foundation Server 0.7.2 的账号接口和 Web 组件。

仅允许一个管理员，移除创建管理员入口。修复会话活跃时间并发更新导致约一分钟后误退登录页的问题。

支持 Host Client 0.9.9 的兼容报告；客户端配置样例保持冻结的 0.9.4 格式，避免将 Server 程序版本误填为 Client 数据格式。
